import React from 'react';

interface Props { }

function EditCocheComponents(props: Props) {
  const { } = props

  return (
    <>
      Component Edit Coche
    </>
  )
}

export default EditCocheComponents
